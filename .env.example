# Ultramarine local environment example.
#
# Copy to `.env` (gitignored) or export in your shell before `make all`/`make all-ipc`.
#
# Archiver (Phase 6)
ULTRAMARINE_ARCHIVER_ENABLED=true
ULTRAMARINE_ARCHIVER_PROVIDER_URL=https://load-s3-agent.load.network
# Cloud deployment currently supports multipart `POST /upload`.
# If your provider supports raw blob uploads, set:
#   ULTRAMARINE_ARCHIVER_UPLOAD_API=blob_upload_v1
#   ULTRAMARINE_ARCHIVER_UPLOAD_PATH=/upload/blob
ULTRAMARINE_ARCHIVER_UPLOAD_API=multipart_upload
# Optional: override the upload path only when needed (e.g. gateway prefix).
# ULTRAMARINE_ARCHIVER_UPLOAD_PATH=/upload
ULTRAMARINE_ARCHIVER_PROVIDER_ID=load-s3-agent
# Bearer token is REQUIRED when enabled; do not commit secrets.
# Obtain a load_acc API key via https://docs.load.network/load-cloud-platform-lcp/ls3-with-load_acc
# and set it in your local `.env` (never commit):
# ULTRAMARINE_ARCHIVER_BEARER_TOKEN=your-load-acc-api-key
ULTRAMARINE_ARCHIVER_RETRY_ATTEMPTS=3
ULTRAMARINE_ARCHIVER_RETRY_BACKOFF_MS=1000
ULTRAMARINE_ARCHIVER_MAX_QUEUE_SIZE=1000
